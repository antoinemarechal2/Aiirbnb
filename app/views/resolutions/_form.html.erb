<%= form_for([@problem, @problem.resolutions.new]) do |f| %>     
 
     <%= f.hidden_field :problem_id, value: @problem.id %>
 
     <%= f.submit "Entrer en discussion avec cette personne", class: "btn btn-primary" %>
 
 <% end %>